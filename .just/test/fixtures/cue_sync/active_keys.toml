# .repo.toml

# TODO: mirrors later

[about]
description = "Old description that should be replaced in place"
topics = ["old", "topics", "that", "should", "be", "replaced"]
license = "GPL-2.0-only"

[urls]
git_ssh = "git@github.com:fini-net/template-repo.git"
web_url = "https://github.com/fini-net/template-repo"

[flags]
claude = true
claude-review = true
copilot-review = true
standard-release = true